import Vue from 'vue'
import Vuex from 'vuex'

Vue.use(Vuex)

export default new Vuex.Store({
  state: {
    sidebarOncollapse:false,    // 侧边栏是否折叠，默认展开
    sidebarWidth:217
  },
  mutations: {
  },
  actions: {
  },
  modules: {
  }
})
